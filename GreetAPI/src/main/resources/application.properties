server.port=8081
#this name will be present in Eureka server.
spring.application.name= GREET-API
# this above line is used to connect  GreetApi to the Admin Server.
spring.boot.admin.client.url=http://localhost:1111/  

# I have define the defaul port 8761 so we are not requird but if we dont define default port above line is mandatory
#eureka.client.service-url.defaultZone: http://localhost:8761/ 

#this is telling that all actuator end point is exposed means we can see all.   actuator is endpoint in a web service
management.endpoints.web.exposure.include=*
